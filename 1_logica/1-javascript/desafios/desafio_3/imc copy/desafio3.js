/* 1 - Crie uma função que calcule o índice de massa corporal (IMC) de uma pessoa, a partir de sua altura, em metros, e peso, em quilogramas, que serão recebidos como parâmetro.*/
// let nome = prompt('Digite o seu nome : ')
// let peso = Number(prompt('Digite o seu peso : '))
// let altura = Number(prompt('Digite sua altura em metro: '))
// let imc 

// const calcularImc = (peso, altura) => {
//     return peso / altura**2
// }
// console.log(`Olá ${nome}, o seu IMC é :`, calcularImc(peso, altura).toFixed(2))

/* 2 - Crie uma função que calcule o valor do fatorial de um número passado como parâmetro.*/
let numero = Number(prompt('Digite um número: '))
let fatorial

const calcularFatorial = num => {
    if (0 >= num || num == null) {
        return fatorial = 1
    }
    else{
        return 'Ola numero maior que 0'
    }

}
console.log(`o fatorial deste número é : `, calcularFatorial(num))


/*Crie uma função que converte um valor em dólar, passado como parâmetro, e retorna o valor equivalente em reais. Para isso, considere a cotação do dólar igual a R$4,80.*/

/*Crie uma função que mostre na tela a área e o perímetro de uma sala retangular, utilizando altura e largura que serão dadas como parâmetro.*/

/*Crie uma função que mostre na tela a área e o perímetro de uma sala circular, utilizando seu raio que será fornecido como parâmetro. Considere Pi = 3,14.*/

/*Crie uma função que mostre na tela a tabuada de um número dado como parâmetro.*/